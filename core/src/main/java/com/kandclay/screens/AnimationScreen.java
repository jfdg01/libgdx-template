package com.kandclay.screens;

import com.kandclay.AudioManager;
import com.kandclay.MyAssetManager;

public class AnimationScreen extends BaseScreen {

    public AnimationScreen(MyAssetManager assetManager, AudioManager audioManager) {
        super(assetManager, audioManager);
    }

    @Override
    public void show() {
        // Implement animation screen setup
    }
}
